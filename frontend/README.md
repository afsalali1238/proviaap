# Prometric Hero - Frontend Client

This is the React + TypeScript application for Prometric Hero.

## 🚀 Getting Started

### Prerequisites
- Node.js (v18+)
- npm

### Installation
```bash
npm install
```

### Local Development
```bash
npm run dev
```

### Build for Production
```bash
npm run build
```

## 🏗 Architecture
This frontend is a **Single Page Application (SPA)** built with Vite.
- **Persistence**: Uses `localStorage` for all user data.
- **Data**: Questions are bundled in `src/features/questions/data/`.
- **Hosting**: Designed for deployment on Vercel (Static).

For full project documentation, see the [root directory README](../../README.md).
